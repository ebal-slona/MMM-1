country_event = {
	id = MY_parliament_support_drift
	hidden = yes
	immediate = {
		if = {
			limit = { par_party_1_support < 10 }
			add_to_variable = { par_party_1_support = -1 }
		}
		if = {
			limit = { par_party_2_support < 10 }
			add_to_variable = { par_party_2_support = -1 }
		}
	}
}
